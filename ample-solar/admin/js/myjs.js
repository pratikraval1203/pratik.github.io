$('form').attr('autocomplete','off');

// $('table').attr('class','table table-bordered nowrap');
$('table').addClass('nowrap');

$("select").select2( {
	// placeholder: "Select Country"
	// allowClear: true
} );
